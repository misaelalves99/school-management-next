// src/types/Student.ts

export type Student = {
  id?: number; // opcional no cadastro
  name: string;
  email: string;
  dateOfBirth: string;
  enrollmentNumber: string;
  phone: string;
  address: string;
};
